# Future Roadmap — llm-engineering-playground

## Planned

- **Import the Month 4 evaluation platform as the quality scorer so comparison tables carry real faithfulness/accuracy columns** — Import the Month 4 evaluation platform as the quality scorer so comparison tables carry real faithfulness/accuracy columns.
- **Prompt-caching-aware cost modelling — show cost with and without cache hits, since that changes the model-choice calculus** — Prompt-caching-aware cost modelling — show cost with and without cache hits, since that changes the model-choice calculus.
- **Batch-API cost mode for workloads that tolerate latency (50% cheaper on supported providers)** — Batch-API cost mode for workloads that tolerate latency (50% cheaper on supported providers).

## Considered and Deferred

_Ideas explicitly rejected, with the reason. This section is often more informative
than the planned list._

## Open Questions

_Things that need data before a decision is possible._
